[project]
name = "smelter-sdk"
version = "0.1.0"
requires-python = ">=3.13"

[tool.hatch.build.targets.wheel]
packages = ["smelter"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
