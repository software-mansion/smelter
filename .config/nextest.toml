[profile.default]
retries = 0

[profile.ci]
retries = 1
slow-timeout = { period = "60s", terminate-after = 3 }
fail-fast = false

[[profile.ci.overrides]]
filter = 'package(/^integration-tests/) and test(/_flaky$/)'
threads-required = 6
retries = 4

[[profile.ci.overrides]]
filter = 'package(/^integration-tests/)'
threads-required = 2
retries = 2
